package com.inf1n1T388.preternaturalism;

public class test {
    public test() {
        /*ResourceLocation reinforcementItemTag = new ResourceLocation(Preternaturalism.MOD_ID, "reinforcementitems");
        Item item = itemInit.abnite_orb;
        Boolean isInTag = ItemTags.getCollection().get(reinforcementItemTag).contains(item);
        if(isInTag){ // EXAMPLE
            item.setDamage(item.getDefaultInstance(), 50);
        }*/
    }
}
